[project]
name = "secretspec"
revision = "1.0"

[profiles.default]
# Requires SecretSpec 0.21+.
MAVEN_CENTRAL_SIGNING_KEY = {
  description = "OpenPGP key used to sign Maven Central releases",
  type = "openpgp_private_key",
  generate = {
    user_id = "Domen Kožar <domen@cachix.org>",
    algorithm = "rsa",
    bits = 4096,
    capabilities = ["sign"],
  },
}
MAVEN_CENTRAL_TOKEN_USERNAME = {
  description = "Maven Central publisher token username",
}
MAVEN_CENTRAL_TOKEN_PASSWORD = {
  description = "Maven Central publisher token password",
}
