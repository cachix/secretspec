#!/usr/bin/env php
<?php

// Thin entry point for the `secretspec-ffi` library installer, exposed as
// `vendor/bin/secretspec-install-lib`. The logic lives in scripts/ so it can
// also be run directly with `php scripts/install-ffi-lib.php` from a checkout.
require __DIR__ . '/../scripts/install-ffi-lib.php';
