name: "Test"

on:
  workflow_call:
  workflow_dispatch:
  pull_request:
  push:
    branches: [ main ]

permissions:
  contents: read

# Do not let superseded revisions occupy the limited macOS runner pool.
concurrency:
  group: ${{ github.workflow }}-test-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Classify changes
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.classify.outputs.code }}
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      if: github.event_name == 'pull_request' || github.event_name == 'push'
      with:
        fetch-depth: 0
        persist-credentials: false
    - name: Detect code changes
      id: classify
      shell: bash
      env:
        BASE_SHA: ${{ github.event.pull_request.base.sha }}
        BEFORE_SHA: ${{ github.event.before }}
        HEAD_SHA: ${{ github.event.pull_request.head.sha }}
      run: |
        case "${{ github.event_name }}" in
          pull_request) range="$BASE_SHA...$HEAD_SHA" ;;
          push)
            if [[ "$BEFORE_SHA" =~ ^0+$ ]]; then
              echo "code=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            range="$BEFORE_SHA..${{ github.sha }}"
            ;;
          *)
            echo "code=true" >> "$GITHUB_OUTPUT"
            exit 0
            ;;
        esac

        code=false
        while IFS= read -r path; do
          case "$path" in
            docs/*|*.md|*.mdx) ;;
            *) code=true; break ;;
          esac
        done < <(git diff --name-only "$range")
        echo "code=$code" >> "$GITHUB_OUTPUT"

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false
    - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
      with:
        node-version: 24
        cache: npm
        cache-dependency-path: docs/package-lock.json
    - name: Install documentation dependencies
      run: npm --prefix docs ci
    - name: Build documentation
      run: npm --prefix docs run build
    - name: Check documentation links
      uses: lycheeverse/lychee-action@e7477775783ea5526144ba13e8db5eec57747ce8 # v2.9.0
      with:
        lycheeVersion: v0.21.0
        workingDirectory: docs
        args: >-
          --root-dir "${{ github.workspace }}/docs/dist"
          --remap "^https://secretspec\\.dev file://${{ github.workspace }}/docs/dist"
          "dist/**/*.html"

  tests:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false
    - name: Download SOPS 3.13.2
      uses: robinraju/release-downloader@28fc21f50d76778e7023361aa1f863e717d3d56f # v1.13
      with:
        repository: getsops/sops
        tag: v3.13.2
        fileName: sops-v3.13.2.${{ runner.os == 'Linux' && 'linux' || 'darwin' }}.${{ runner.arch == 'ARM64' && 'arm64' || 'amd64' }}
        out-file-path: sops-bin
    - name: Add SOPS to PATH
      shell: bash
      run: |
        install -m 0755 sops-bin/sops-* sops-bin/sops
        echo "$PWD/sops-bin" >> "$GITHUB_PATH"
    - name: Install Rust (pinned by rust-toolchain.toml)
      run: |
        rustup toolchain install
        rustup component add rustfmt clippy
    - name: Cache Rust builds
      uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
      with:
        key: ${{ matrix.os }}
    # libsecretspec-resolver takes yyjson from the system rather than vendoring
    # it, and this job has no Nix to supply it. The Rust conformance package
    # compiles the same C sources, so this has to precede every cargo step too.
    - name: Install yyjson
      shell: bash
      run: bash scripts/install-yyjson.sh

    # Formatting and linting are platform-independent; run them once on Linux.
    # The PHP extension is checked in the SDK workflow, where PHP headers are
    # available; this native Rust job does not need the full multi-SDK shell.
    - name: Check formatting
      if: runner.os == 'Linux'
      run: cargo fmt --all -- --check
    - name: Run Clippy
      if: runner.os == 'Linux'
      run: cargo clippy --workspace --exclude secretspec-php-native
    - name: Run tests
      run: cargo test --workspace --exclude secretspec-php-native
    # The C client is standalone C11, so it is built with its own build systems
    # rather than the devenv shell: this job installs Rust directly and has no
    # Nix, and Windows runners cannot run devenv at all.
    - name: Install Meson and Ninja
      shell: bash
      # pipx rather than pip: the macOS runner's Python is externally managed
      # (PEP 668), so a plain --user install is refused there.
      run: |
        pipx install meson
        pipx install ninja
    - name: Build standalone C IPC client with Meson
      shell: bash
      run: |
        meson setup libsecretspec-resolver/build-meson libsecretspec-resolver
        meson compile -C libsecretspec-resolver/build-meson
        meson test -C libsecretspec-resolver/build-meson --print-errorlogs
    - name: Build standalone C IPC client with CMake
      shell: bash
      run: |
        cmake -S libsecretspec-resolver -B libsecretspec-resolver/build-cmake
        cmake --build libsecretspec-resolver/build-cmake
        ctest --test-dir libsecretspec-resolver/build-cmake --output-on-failure

  # Each optional provider must compile on its own, not just in the default
  # feature set. `cargo test` above cannot catch a missing feature gate: it
  # builds with every provider enabled and with `cfg(test)` on, and shared
  # helpers gated on `any(feature = "...", test)` are reachable under either.
  # A provider that forgets to add itself to such a gate only breaks for the
  # user who builds it standalone, which is precisely who `--features` is for.
  features:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false
    - name: Install Rust
      run: rustup toolchain install stable --profile minimal
    - name: Cache Rust builds
      uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
    - name: Check each provider feature standalone
      run: |
        set -euo pipefail
        for feature in cli keyring keeper gcsm awssm vault openbao infisical bws akv aac sops bw; do
          echo "::group::$feature"
          cargo check --package secretspec --no-default-features --features "$feature"
          echo "::endgroup::"
        done

  package:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false
    - name: Install Rust
      run: rustup toolchain install stable --profile minimal
    - name: Verify the published crate contains its schema fixture
      shell: bash
      run: |
        set -euo pipefail
        fixture="secretspec/src/fixtures/resolution-report.schema.json"
        cmp schema/resolution-report.schema.json "$fixture"
        cargo package --package secretspec --list |
          grep -Fx 'src/fixtures/resolution-report.schema.json'

  # Windows runners can't run Nix/devenv, so the suite runs here natively via
  # rustup + cargo. This exists specifically to catch Windows-only path handling
  # regressions (e.g. config discovery and relative `-f` paths, issue #59), which
  # the Ubuntu/macOS Rust job above can never observe.
  windows:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false
    - name: Download SOPS 3.13.2
      uses: robinraju/release-downloader@28fc21f50d76778e7023361aa1f863e717d3d56f # v1.13
      with:
        repository: getsops/sops
        tag: v3.13.2
        fileName: sops-v3.13.2.amd64.exe
        out-file-path: sops-bin

    - name: Add SOPS to PATH
      shell: pwsh
      run: |
        Rename-Item -Path "sops-bin/sops-v3.13.2.amd64.exe" -NewName "sops.exe"
        (Resolve-Path "sops-bin").Path |
          Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
    - name: Install Rust
      run: rustup toolchain install stable --profile minimal
    - name: Cache Rust builds
      uses: Swatinem/rust-cache@f0d9c3887740aee45f6153b24b3a6b815192ec16 # v2
    # This runner has no pkg-config, so the script also exports the explicit
    # YYJSON_INCLUDE_DIR / YYJSON_LIB_DIR pair that the conformance build script
    # falls back to.
    - name: Install yyjson
      shell: bash
      run: bash scripts/install-yyjson.sh
    - name: cargo test
      # Exclude secretspec-php-native: it is an ext-php-rs extension that needs a
      # PHP dev toolchain (php-config/headers) this bare runner does not have, and
      # ext-php-rs rejects the runner's PHP version anyway. The PHP SDK is built
      # and tested by sdks.yml / php-ext.yml instead.
      run: cargo test --workspace --exclude secretspec-php-native
    # The Rust conformance build compiles the Windows C launcher, but only the
    # standalone suite executes its process/environment regressions. Keep this
    # native CMake run on Windows so ACL-adjacent launch behavior cannot be
    # "covered" solely by Linux and macOS tests of the POSIX implementation.
    - name: Build and test standalone C IPC client
      shell: pwsh
      run: |
        cmake -S libsecretspec-resolver -B libsecretspec-resolver/build-cmake
        cmake --build libsecretspec-resolver/build-cmake --config Release
        ctest --test-dir libsecretspec-resolver/build-cmake `
          --build-config Release --output-on-failure
